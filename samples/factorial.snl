program factorial
    var integer result;
        integer n;

    procedure fact(integer m);
    begin
        if m < 2
        then
            result := 1
        else
            fact(m - 1);
            result := m * result
        fi
    end

begin
    n := 5;
    fact(n);
    write(result)
end.
