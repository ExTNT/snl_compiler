name: Rust Multi-Platform Build

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: 'Release tag (optional, e.g. v1.0.0)'
        required: false
        default: ''
      build_mode:
        description: 'Build mode'
        required: true
        default: 'release'
        type: choice
        options:
          - release
          - debug

env:
  CARGO_TERM_COLOR: always

jobs:
  # ========================
  # Linux 构建
  # ========================
  build-linux:
    name: Build - Linux (x86_64-unknown-linux-gnu)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-gnu

      - name: Cache Cargo registry
        uses: actions/cache@v4
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-x86_64-gnu-cargo-registry-${{ hashFiles('**/Cargo.lock') }}

      - name: Cache Cargo index
        uses: actions/cache@v4
        with:
          path: ~/.cargo/git
          key: ${{ runner.os }}-x86_64-gnu-cargo-index-${{ hashFiles('**/Cargo.lock') }}

      - name: Cache Cargo build
        uses: actions/cache@v4
        with:
          path: target
          key: ${{ runner.os }}-x86_64-gnu-cargo-build-${{ hashFiles('**/Cargo.lock') }}

      - name: Build
        run: cargo build --${{ github.event.inputs.build_mode }} --target x86_64-unknown-linux-gnu

      - name: Package artifact
        run: |
          mkdir -p artifacts
          BINARY_NAME=$(cargo metadata --no-deps --format-version 1 | jq -r '.packages[0].name')
          if [ "${{ github.event.inputs.build_mode }}" = "release" ]; then
            cp target/x86_64-unknown-linux-gnu/release/${BINARY_NAME} artifacts/${BINARY_NAME}-x86_64-unknown-linux-gnu
          else
            cp target/x86_64-unknown-linux-gnu/debug/${BINARY_NAME} artifacts/${BINARY_NAME}-x86_64-unknown-linux-gnu-debug
          fi
          chmod +x artifacts/*

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: x86_64-unknown-linux-gnu
          path: artifacts/*
          if-no-files-found: error

  # ========================
  # macOS 构建
  # ========================
  build-macos:
    name: Build - macOS (${{ matrix.target }})
    runs-on: macos-latest
    strategy:
      fail-fast: false
      matrix:
        target:
          - x86_64-apple-darwin
          - aarch64-apple-darwin

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Cargo registry
        uses: actions/cache@v4
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-${{ matrix.target }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}

      - name: Cache Cargo index
        uses: actions/cache@v4
        with:
          path: ~/.cargo/git
          key: ${{ runner.os }}-${{ matrix.target }}-cargo-index-${{ hashFiles('**/Cargo.lock') }}

      - name: Cache Cargo build
        uses: actions/cache@v4
        with:
          path: target
          key: ${{ runner.os }}-${{ matrix.target }}-cargo-build-${{ hashFiles('**/Cargo.lock') }}

      - name: Build
        run: cargo build --${{ github.event.inputs.build_mode }} --target ${{ matrix.target }}

      - name: Package artifact
        run: |
          mkdir -p artifacts
          BINARY_NAME=$(cargo metadata --no-deps --format-version 1 | jq -r '.packages[0].name')
          if [ "${{ github.event.inputs.build_mode }}" = "release" ]; then
            cp target/${{ matrix.target }}/release/${BINARY_NAME} artifacts/${BINARY_NAME}-${{ matrix.target }}
          else
            cp target/${{ matrix.target }}/debug/${BINARY_NAME} artifacts/${BINARY_NAME}-${{ matrix.target }}-debug
          fi
          chmod +x artifacts/*

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.target }}
          path: artifacts/*
          if-no-files-found: error

  # ========================
  # Windows 构建
  # ========================
  build-windows:
    name: Build - Windows (${{ matrix.target }})
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        target:
          - x86_64-pc-windows-msvc
          - i686-pc-windows-msvc
          - aarch64-pc-windows-msvc

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Cargo registry
        uses: actions/cache@v4
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-${{ matrix.target }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}

      - name: Cache index
        uses: actions/cache@v4
        with:
          path: ~/.cargo/git
          key: ${{ runner.os }}-${{ matrix.target }}-cargo-index-${{ hashFiles('**/Cargo.lock') }}

      - name: Cache Cargo build
        uses: actions/cache@v4
        with:
          path: target
          key: ${{ runner.os }}-${{ matrix.target }}-cargo-build-${{ hashFiles('**/Cargo.lock') }}

      - name: Build
        run: cargo build --${{ github.event.inputs.build_mode }} --target ${{ matrix.target }}

      - name: Package artifact
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path artifacts
          $metadata = cargo metadata --no-deps --format-version 1 | ConvertFrom-Json
          $binaryName = $metadata.packages[0].name
          $mode = "${{ github.event.inputs.build_mode }}"
          if ($mode -eq "release") {
            Copy-Item "target/${{ matrix.target }}/release/${binaryName}.exe" "artifacts/${binaryName}-${{ matrix.target }}.exe"
          } else {
            Copy-Item "target/${{ matrix.target }}/debug/${binaryName}.exe" "artifacts/${binaryName}-${{ matrix.target }}-debug.exe"
          }

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.target }}
          path: artifacts/*
          if-no-files-found: error

  # ========================
  # 创建 Release（可选）
  # ========================
  create-release:
    name: Create Release
    needs: [build-linux, build-macos, build-windows]
    if: github.event.inputs.release_tag != ''
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: all-artifacts
          merge-multiple: true

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.release_tag }}
          name: Release ${{ github.event.inputs.release_tag }}
          files: all-artifacts/*
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
